trigger: 
- main

pool:
 vmImage: windows-latest

steps:
    - task: Docker@2
      inputs:
        command: 'build'
        Dockerfile: 'build/Dockerfile.build'
        buildContext: 'build'
        tags: 'my-build-tools'